# Harness pointer

The canonical guidance lives in the OMP-native context file. This file exists only so
Claude Code — if it is ever opened again — sees the same instructions instead of a stale
fork of them.

Edit `nixos-config/omp/AGENTS.md`, never this file.

@~/nixos-config/omp/AGENTS.md

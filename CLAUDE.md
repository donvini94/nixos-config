# Project context pointer

The canonical guidance for this repository lives in `AGENTS.md`, the cross-harness context
file. This file exists only so Claude Code — if it is ever opened here — sees the same
instructions instead of a stale fork of them.

Edit `AGENTS.md`, never this file.

@AGENTS.md
